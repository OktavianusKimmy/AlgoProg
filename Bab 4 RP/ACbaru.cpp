#include <stdio.h>

int main(){
	int t, jenis, uang, harga1=0, harga2=0, palingMurah=0, count=0;
	scanf("%d", &t);
	for(int i=1; i<=t; i++){
		scanf("%d %d", &jenis, &uang);

		for(int a=0; a<jenis; a++){
			scanf("%d", &harga1[k]);
		}
		for(int k=0;k<)
		
		printf("%d", uang/count);
	}
	return 0;
}
